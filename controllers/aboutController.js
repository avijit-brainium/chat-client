app.controller('aboutCtrl', function ($scope, UserService) {
    angular.element(document).ready(function () {
        
        
                
        /*var peer = new Peer('avijit_amstech', {
            host: 'localhost',
            port: 9000,
            path: '/',

            // Set highest debug level (log everything!).
            debug: 3,
        });
        
        $scope.my_peer = {};
        
        peer.on('open', function(id){
            //alert(id);
            //$scope.my_peer.id = id;
            //console.log("$scope.my_peer.id : ", $scope.my_peer.id);
            $('#peer_id').val(id);
        });*/
    }); 
});

